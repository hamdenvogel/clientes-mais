# Exemplo — NÃO commitar secret real.
# Gere o secret a partir do .env:
#   .\infra\k3d\create-secret.ps1
apiVersion: v1
kind: Secret
metadata:
  name: clientes-api-secrets
  namespace: clientes-mais
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: "CHANGE_ME"
  SPRING_RABBITMQ_PASSWORD: "CHANGE_ME"
  SECURITY_JWT_SIGNING_KEY: "CHANGE_ME"
  SECURITY_JWT_CHAVE_ASSINATURA: "CHANGE_ME"
  APP_REGISTRATION_TOKEN_SECRET: "CHANGE_ME"
  POSTGRES_PASSWORD: "CHANGE_ME"
  RABBITMQ_PASSWORD: "CHANGE_ME"
