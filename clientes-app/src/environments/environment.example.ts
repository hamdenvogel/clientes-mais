/**
 * Exemplo — copie valores para `environment.ts` / `environment.prod.ts` localmente.
 * Não commite chaves reais.
 */
export const environmentExample = {
  production: false,
  apiURLBase: 'http://localhost:8080',
  recaptchaSiteKey: 'YOUR_RECAPTCHA_SITE_KEY',
  obterTokenUrl: '/api/auth/signin'
};
